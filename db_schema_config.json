{
  "REPORTING.orders": {
    "description": "Contains order transactions and purchase history",
    "columns": {
      "id": "Primary key, unique order identifier",
      "user_id": "Foreign key to users table, identifies the customer",
      "product_id": "Foreign key to products table, identifies the ordered item",
      "quantity": "Number of items ordered",
      "order_date": "Date and time when order was placed",
      "status": "Order status: pending, completed, or cancelled"
    },
    "foreign_keys": [
      {
        "column": "user_id",
        "foreign_column": "REPORTING.users.id"
      },
      {
        "column": "product_id",
        "foreign_column": "REPORTING.products.id"
      }
    ]
  }
}
